 <article class="card mb-2" >
    <div class="card-body">
      <p>{{this.origin}}</p> 
      <p>{{this.shortURL}}</p>
    <button data-short="{{this.shortURL}}" class="btn btn-primary">enviar</button> 
    <a href="/editar/{{this._id}}" class="btn btn-warning">editar</a>
    <a href="/eliminar/{{this._id}} "class="btn btn-danger">eliminar</a> 
    </div>
  </article>